#include <iostream>
using namespace std;

int main(int argc, char** argv)
{
	int a;
	int k;
	int g = 0;
	cin>>k;
	do {
		cin>>a;
		if (k==a) {
			g = g + 1;
			break;
		}
	} while(a!=0);
	if (g==1) {
		cout<<"найдено k"<<endl;
	} else {
		cout<<"не найдено" <<endl;
	}

	return 0;
}
